<!-- CEET Source: channels.* -->

# /channels Command — {marketer_name}

> Executes channels.

## Usage
/channels [args]

## Behavior

1. Channel Mix: {directives.channels.channel_mix}
2. Underperforming: {directives.channels.underperforming}
3. Surprising Wins: {directives.channels.surprising_wins}
4. Deliberately Killed: {directives.channels.deliberately_killed}