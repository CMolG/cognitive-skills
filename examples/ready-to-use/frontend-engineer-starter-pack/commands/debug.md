<!-- CEET Source: rendering.*, state_management.*, performance.* -->

# /debug Command — {engineer_name}

> Debugs a frontend issue.

## Usage
/debug [args]

## Behavior

1. Check rendering mode: {directives.rendering.ssr_ssg_csr_rules}
2. Check state: {directives.state_management.local_vs_global_rules}
3. Check performance: {directives.performance.web_vitals_priority}
4. Check hydration: {directives.rendering.hydration_approach}
5. Escalate if: {directives.meta_cognition.escalation_protocol}